import * as theme from './theme';
import * as iconPath from './iconpath';

const SCREENS = {
  //  ---- common ----
  LOGIN: 'LoginSCreen',
  ONBOARDING: 'OnboardingScreen',
  FORGOT_PASSWORD: 'ForgotPasswordScreen',
};
export {theme, iconPath, SCREENS};
